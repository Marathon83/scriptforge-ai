# Override the backend URL for production builds on real devices or hosted deployments.
# Emulator/simulator addresses are handled automatically in src/api/client.js.
# e.g. VITE_API_URL=https://api.scriptforge.example.com
