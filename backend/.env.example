# ScriptForge AI — Backend environment variables
# Copy this to .env and fill in your values.

# CORS: comma-separated list of additional allowed origins (your production domain)
# e.g. CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
CORS_ORIGINS=
